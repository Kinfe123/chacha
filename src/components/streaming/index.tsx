interface StreamProps {
    user: string ,
    stream: string ,
    isFollowing: string
}

export const StreamPlayer = ({stream , user , isFollowing}: StreamProps) => {
    return (
        <div>

        </div>
    )
}